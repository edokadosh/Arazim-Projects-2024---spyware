
#ifndef FUNCODES_ENUM_H
#define FUNCODES_ENUM_H

typedef enum FunCode {
    WRITE_FILE = 1,
    DELETE_FILE = 2,
    RUN_BASH = 3,
    HIDER_SETUP = 4,
    RunContraption = 5,
    HaltContraption = 6,
    SUICIDE = 7,
} FunCode;

#endif