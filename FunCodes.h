
#ifndef FUNCODES_ENUM_H
#define FUNCODES_ENUM_H

typedef enum FunCode {
    WRITE_FILE = 0,
    DELETE_FILE,
    RUN_BASH,
    HIDER_SETUP,
    HIDDEN_UPLOAD,
    HIDDEN_DELETE,
    HIDDEN_RUN,
    HIDDEN_LIST
} FunCode;

#endif